<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/GatherInfo.css" />
</head>
<body>
    <div class="container">
        <header class="form-header">
            <h1 class="form-title">Checkout</h1>
        </header>

        <form asp-action="ProcessCheckout" asp-controller="Payment" method="post">
            <div class="form-group">
                <label for="Name">Пълно име</label>
                <input type="text" id="Name" name="Name" class="modern-input" required />
            </div>

            <div class="form-group">
                <label for="Email">Email</label>
                <input type="email" id="Email" name="Email" class="modern-input" required />
            </div>

            <div class="form-group">
                <label for="Phone">Телефон за връзка</label>
                <input type="tel" id="Phone" name="Phone" class="modern-input" placeholder="+359 878121212" required />
            </div>

            <div class="form-group">
                <label for="Address">Адрес</label>
                <textarea id="Address" name="Address" rows="4" class="modern-input" placeholder="Въведете адрес на получаване" required></textarea>
            </div>

            <div class="payment-options">
                <h3 class="payment-title">Начин на плащане</h3>
                <div class="payment-cards">
                    <label class="payment-card">
                        <input type="radio" name="PaymentMethod" value="card" required />
                        <div class="payment-content">
                            <span class="payment-icon">💳</span>
                            <span class="payment-label">Карта</span>
                        </div>
                    </label>
                    <label class="payment-card">
                        <input type="radio" name="PaymentMethod" value="cod" />
                        <div class="payment-content">
                            <span class="payment-icon">📦</span>
                            <span class="payment-label">Наложен платеж</span>
                        </div>
                    </label>
                </div>
            </div>

            <button type="submit" class="submit-btn">
                <span class="btn-text">Подвърди поръчка →</span>
            </button>
        </form>
    </div>
</body>
</html>